package com.bca.cmt.repository;

import com.bca.cmt.model.ProductT;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProductTRepository extends JpaRepository<ProductT, Long> {
}
